/**
 * CITADEL — обработчик стилей.
 * Собирает оформление и добавляет совместимость
 * со всеми браузерами.
 */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;